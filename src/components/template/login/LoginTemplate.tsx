import React from 'react';
import { Heading } from '@/components/atoms';

const LoginTemplate = () => {
  return (
    <div>
      <Heading level={2}>로그인 페이지 제목</Heading>
    </div>
  );
};

export default LoginTemplate;
